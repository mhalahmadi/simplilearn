function ProjectDetails(props) {

    return ( <
        div >
        <
        p > Project Name { props.pname }, technologies { props.trch } < /p> < /
        div >
    )
}

export default ProjectDetails;